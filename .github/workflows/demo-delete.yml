name: Demo delete
on:
  workflow_call:
    inputs:
      app:
        required: false
        default: ${{ github.event.repository.name }}
        type: string
      base-path:
        required: true
        type: string

jobs:
  delete-demo-app:
    if: startsWith(github.event.ref, 'demo-')
    name: Delete demo branch dev-gcp
    runs-on: ubuntu-latest
    env:
      DEMO_APPNAME: demo-${{ inputs.app }}-${{ github.event.ref }}
    steps:
      - uses: actions/checkout@v3
      - name: Skaler ned app i dev-gcp
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          DEPLOY_SERVER: deploy.nais.io:443
          RESOURCE: nais/app/demo.yaml
          IMAGE: ghcr.io/navikt/${{ inputs.app }}:latest
          VAR: appname=${{ env.DEMO_APPNAME }},replicas=0,branchState=deleted,ingress=https://${{ env.DEMO_APPNAME }}.ekstern.dev.nav.no/${{inputs.base-path}}
