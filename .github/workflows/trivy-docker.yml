name: Trivy docker
on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string


jobs:
  snyk-test-docker:
    if: github.ref_name == 'testbranch' || startsWith(github.ref_name, 'dev-')
    runs-on: ubuntu-latest
    steps:
      - uses: nais/login@v0
        with:
          project_id: ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}
          identity_provider: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
          team: flex

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ inputs.image }}
          ignore-unfixed: true
          vuln-type: 'os,library' # skal vi bare ha os? eller også npm biblioteker?
          severity: 'CRITICAL,HIGH' # legge inn low også her?
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v2
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ inputs.image }}
          format: 'table'
          exit-code: '1' 
          ignore-unfixed: true
          vuln-type: 'os,library' # skal vi bare ha os? eller også npm biblioteker?
          severity: 'CRITICAL,HIGH'
        
      - name: Slack Notification
        uses: rtCamp/action-slack-notify@12e36fc18b0689399306c2e0b3e0f2978b7f1ee7
        if: failure() && github.ref_name == 'master'
        env:
          SLACK_COLOR: '#ff0000'
          SLACK_MESSAGE: ${{ github.event.repository.name }} bygget feiler på master
          SLACK_TITLE: 'Auda :alert:'

      